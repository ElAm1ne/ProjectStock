import { StockSearchHistory } from './stock-search-history';

describe('StockSearchHistory', () => {
  it('should create an instance', () => {
    expect(new StockSearchHistory()).toBeTruthy();
  });
});
